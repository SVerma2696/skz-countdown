Drop group photos here to replace the auto-generated placeholders.
Name them 1.png, 2.png, 3.png, ... — the app cycles through however many
it finds. Landscape shots (e.g. 1200x800) look best. Missing files are fine;
the app falls back to a clean placeholder.