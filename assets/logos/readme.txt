OPTIONAL: drop the official brand logos here to replace the simple lettered
placeholders the app draws by default. Use these exact file names (PNG,
JPG, and WEBP all work):

  spotify-logo.png       (Spotify)
  applemusic-logo.webp   (Apple Music)
  skzshoplogo.webp       (Stray Kids Shop)
  github-logo.webp       (the "View on GitHub" button)

Small square pictures with a transparent background look best (e.g. 64x64).
Whatever shape you use, the WHOLE logo is always shown — shrunk to fit if
needed, never stretched or cut off.

These are third-party trademarks, so they are NOT bundled with this project —
add them yourself if you want them. The app works fine without them.
